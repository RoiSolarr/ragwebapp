import Link from "next/link";
import { redirect } from "next/navigation";
import { createClient } from "@/lib/supabase/server";
import { prisma } from "@/lib/prisma";
import { createWorkspace } from "@/app/actions/workspace-actions";
import { SiteHeader } from "@/components/site-header";

export default async function ChatPage() {
  const supabase = await createClient();
  const { data: { user } } = await supabase.auth.getUser();
  if (!user) redirect("/login");
  const workspaces = await prisma.workspace.findMany({
    where: { members: { some: { userId: user.id } } },
    include: { _count: { select: { documents: true, chatMessages: true } } },
    orderBy: { updatedAt: "desc" },
  });

  return <div className="min-h-screen bg-paper"><SiteHeader variant="app" /><main className="mx-auto w-full max-w-6xl px-5 pb-20 pt-8 sm:px-6 sm:pt-12"><div className="reveal border-b border-line pb-8"><p className="text-xs font-bold uppercase tracking-[.18em] text-indigo">Grounded assistant</p><h1 className="mt-3 font-serif text-4xl font-semibold tracking-[-.03em] text-ink sm:text-5xl">Chat</h1><p className="mt-3 max-w-lg text-ink-muted">Choose a workspace to ask questions about its documents, or create a new workspace to get started.</p></div><form action={createWorkspace} className="reveal mt-8 flex w-full max-w-xl gap-2" style={{ animationDelay: "80ms" }}><input name="name" placeholder="Name a new workspace" aria-label="New workspace name" className="min-w-0 flex-1 rounded-xl border border-line bg-paper-raised px-3.5 py-3 text-sm text-ink outline-none placeholder:text-ink-muted focus:border-highlight focus:ring-4 focus:ring-highlight/10" required /><button type="submit" className="inline-flex h-12 shrink-0 items-center justify-center gap-2 rounded-xl bg-gradient-to-r from-[#4c43ce] to-[#35d7ff] px-5 text-sm font-semibold text-white shadow-[0_8px_20px_rgba(53,215,255,.18)] transition-all hover:-translate-y-0.5 hover:brightness-110">Create <span aria-hidden="true">+</span></button></form><section className="reveal mt-10" style={{ animationDelay: "140ms" }}><div className="flex items-end justify-between gap-3"><div><p className="text-xs font-bold uppercase tracking-[.16em] text-indigo">Your workspaces</p><h2 className="mt-2 text-2xl font-semibold text-ink">Select a workspace</h2></div><span className="text-xs text-ink-muted">{workspaces.length} available</span></div>{workspaces.length === 0 ? <div className="mt-5 rounded-2xl border border-dashed border-indigo/30 bg-paper-raised p-8 text-center"><p className="text-sm text-ink-muted">Create a workspace above to open your first grounded chat.</p></div> : <div className="mt-5 grid gap-4 sm:grid-cols-2 lg:grid-cols-3">{workspaces.map((workspace, index) => <Link key={workspace.id} href={`/workspaces/${workspace.id}`} className="group rounded-[1.25rem] border border-line bg-paper-raised p-6 shadow-[0_4px_14px_rgba(19,44,58,.05)] transition-all hover:-translate-y-1 hover:border-highlight/50 hover:shadow-[0_16px_30px_rgba(53,215,255,.12)]" style={{ animationDelay: `${index * 70}ms` }}><div className="flex items-center justify-between gap-3"><p className="text-xs font-bold uppercase tracking-[.16em] text-indigo">Workspace {String(index + 1).padStart(2, "0")}</p><span className="h-2 w-2 rounded-full bg-highlight" aria-hidden="true" /></div><h3 className="mt-8 truncate font-serif text-xl font-semibold text-ink">{workspace.name}</h3><p className="mt-2 text-sm text-ink-muted">{workspace._count.documents} documents · {Math.floor(workspace._count.chatMessages / 2)} question pairs</p><span className="mt-8 inline-flex text-xs font-bold uppercase tracking-[.14em] text-ink-muted transition-colors group-hover:text-indigo">Open chat <span className="ml-2 transition-transform group-hover:translate-x-1" aria-hidden="true">→</span></span></Link>)}</div>}</section></main></div>;
}
